
#### Recently Closed

1. **Pure evaluation exception carries Pure stack trace and Pure exception kind** — Shipped 2026-05-18. `crates/jni/src/exception.rs` introduces `ExceptionPayload` (pure data) + `pure_exception_to_payload` (pure function, cargo-testable) that flattens a runtime `PureException` into the strings the Java rich constructor consumes: `message`, `kind` ("EXECUTION_ERROR" / "ASSERTION_FAILED" / "CONSTRAINT_VIOLATION"), nullable `sourceInfo`, `callStack[]` (innermost-first), and constraint-only `constraintId` / `constraintKind` ("CLASS" / "PRE" / "POST") / `ownerFqn`. `crates/jni/src/lib.rs::throw_pure_exception` is the JNI side: builds the Java exception via `env.new_object` against `PureRustEvaluationException`'s new rich constructor, falling back to the legacy message-only `env.throw_new` if `new_object` fails (cdylib-older-than-JAR forward-compat). `JniContext::evaluate` and `get_property` now return `Result<Value, PureException>` (was `Result<Value, String>`); FFI-boundary errors get a synthesised `PureException` with a `"<ffi>"` source so the throw path stays uniform. Java side: `PureRustEvaluationException` keeps the message-only constructor and adds a 7-arg rich constructor plus `getKind`, `getSourceInfo`, `getCallStack`, `getConstraintId`, `getConstraintKind`, `getOwnerFqn` accessors. JUnit pins in `TestRustEvaluation.testEvaluationExceptionCarriesKindField` and `TestRustEvaluation.testConstraintViolationCarriesStructuredFields` (the latter calls `LA_triggerInheritedConstraintViolation` added to `platform/pure/.../new.pure` to trigger the inherited-constraint violation end-to-end). Cargo payload pins: `exception::tests::*` (4 tests).
